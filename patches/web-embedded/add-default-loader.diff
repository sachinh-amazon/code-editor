Index: third-party-src/build/gulpfile.vscode.web.ts
===================================================================
--- third-party-src.orig/build/gulpfile.vscode.web.ts
+++ third-party-src/build/gulpfile.vscode.web.ts
@@ -182,6 +182,10 @@ function packageTask(sourceFolderName: s
 			gulp.src('resources/server/code-192.png', { base: 'resources/server' }),
 			gulp.src('resources/server/code-512.png', { base: 'resources/server' })
 		);
+		const indexHtml = gulp.src('resources/server/index.html', { base: 'resources/server' });
+
+		const productJsonStream = gulp.src(['product.json'], { base: '.' })
+			.pipe(jsonEditor({ commit, date: readISODate('out-build'), version }));
 
 		const all = es.merge(
 			packageJsonStream,
@@ -190,7 +194,9 @@ function packageTask(sourceFolderName: s
 			deps,
 			favicon,
 			manifest,
-			pwaicons
+			pwaicons,
+			indexHtml,
+			productJsonStream
 		);
 
 		const result = all
